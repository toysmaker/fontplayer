/**
 * 识别图片主区轮播：与原 ThumbnailEditPanel.vue 中 previewList.length 一致（5 档）
 */
export const PIC_PREVIEW_SLIDE_COUNT = 5

/** 主区横向排列的画布数量：preview1 + thumbnail + bitmap + contours + curves + preview2 */
export const PIC_CAROUSEL_PANEL_COUNT = 6
