# E2E 静态夹具

- `minimal-project-file.json`：符合 `IFile` 外形的**壳数据**，用于校验 JSON 与文档说明；字符完整轮廓在运行时通常存 IndexedDB，完整流程请使用应用内「打开工程」导出真实 `.fontplayer` / JSON。
- 在 Playwright 中可用 `readFileSync` + `JSON.parse` 做结构校验，或通过后续 `fileHandlers.openFile` 接入（桌面端 Tauri）。
